# Data folder

This folder is intentionally empty in the published repository.

- Raw COSMO-REA6 GRIB/BZ2 archives and OPSD registers are too large for GitHub.
- Pre-built compact products (Parquet/NetCDF) live in `app_assets/2018/`.
- To rebuild, run the numbered scripts in `scripts/` after setting up the conda environment.
